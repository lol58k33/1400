1.1
#include <iostream>
#include <iomanip>
#include <cmath>

int main() {
    std::cout << std::fixed << std::setprecision(2) << M_PI << std::endl;
    return 0;
}
1.2
#include <iostream>
#include <iomanip>
#include <cmath>

int main() {
    std::cout << std::fixed << std::setprecision(1) << M_E << std::endl;
    return 0;
}
1.3
#include <iostream>

int main() {
    int number;
    std::cout << "Введите число: ";
    std::cin >> number;
    std::cout << "Вы ввели число " << number << std::endl;
    return 0;
}
1.4
#include <iostream>

int main() {
    int number;
    std::cout << "Введите число: ";
    std::cin >> number;
    std::cout << number << " вот какое число Вы ввели" << std::endl;
    return 0;
}
1.5
#include <iostream>

int main() {
    std::cout << 1 << " " << 13 << " " << 49 << std::endl;
    return 0;
}
1.6
#include <iostream>

int main() {
    std::cout << 7 << "  " << 15 << "  " << 100 << std::endl;
    return 0;
}
1.7
#include <iostream>

int main() {
    int num1, num2, num3;
    std::cout << "Введите три числа: ";
    std::cin >> num1 >> num2 >> num3;
    std::cout << num1 << "  " << num2 << "  " << num3 << std::endl;
    return 0;
}
1.8
#include <iostream>

int main() {
    int num1, num2, num3, num4;
    std::cout << "Введите четыре числа: ";
    std::cin >> num1 >> num2 >> num3 >> num4;
    std::cout << num1 << " " << num2 << " " << num3 << " " << num4 << std::endl;
    return 0;
}
1.9
#include <iostream>

int main() {
    std::cout << 50 << std::endl;
    std::cout << 10 << std::endl;
    return 0;
}
1.10
#include <iostream>

int main() {
    std::cout << 5 << std::endl;
    std::cout << 10 << std::endl;
    std::cout << 21 << std::endl;
    return 0;
}
1.11
#include <iostream>

int main() {
    int num1, num2, num3, num4;
    std::cout << "Введите четыре числа: ";
    std::cin >> num1 >> num2 >> num3 >> num4;
    std::cout << num1 << std::endl;
    std::cout << num2 << std::endl;
    std::cout << num3 << std::endl;
    std::cout << num4 << std::endl;
    return 0;
}
1.12
#include <iostream>

int main() {
    int v, x, y;
    std::cout << "Введите значения для v, x, y: ";
    std::cin >> v >> x >> y;
    std::cout << "5 10" << std::endl;
    std::cout << "7 см" << std::endl;
    std::cout << "100 г" << std::endl;
    std::cout << "1949 " << v << std::endl;
    std::cout << x << " 25" << std::endl;
    std::cout << x << y << std::endl;
    return 0;
}
1.13
#include <iostream>

int main() {
    int a, b, x, y;
    std::cout << "Введите значения для a, b, x, y: ";
    std::cin >> a >> b >> x >> y;
    std::cout << "2 кг" << std::endl;
    std::cout << "13 17" << std::endl;
    std::cout << a << " 1" << std::endl;
    std::cout << "19 " << b << std::endl;
    std::cout << x << y << std::endl;
    std::cout << "5 " << y << std::endl;
    return 0;
}
1.14
a) 2 * x
б) sin(x)
в) pow(a, 2)
г) sqrt(x)
д) abs(n)
е) 5 * cos(y)
ж) -7.5 * pow(a, 2)
з) 3 * sqrt(x)
и) sin(alpha) * cos(beta) + cos(alpha) * sin(beta)
к) a * sqrt(2 * b)
л) 3 * sin(2 * alpha) * cos(3 * beta)
м) -5 * sqrt(x + sqrt(y))
1.15
a) (-1) / pow(x, 2)
б) a / (b * c)
в) a / b * c
г) (a + b) / 2
д) 5.45 * (a + 2 * b) / (2 - a)
е) (-b + sqrt(pow(b, 2) - 4 * a * c)) / (2 * a)
ж) (-b + 1 / a) / (2 / c)
з) 1 / (1 + (a + b) / 2)
и) 1 / (1 + 1 / (2 + 1 / (2 + 3 / 5)))
к) pow(2, pow(m, n))
1.16
a) \(\frac{a}{b\cdot c}\) б) \(\frac{a\cdot b}{c}\) в) \(\frac{a}{b}\cdot c\) г) \(a+\frac{b}{c}\) д) \(a+\frac{b}{c}\) е) \(a+\frac{b}{b}+c\) ж) \(a+\frac{b}{b}+c\) з) \(\frac{a}{\sin b}\) и) \(\frac{1}{2}\cdot a\cdot b\cdot \sin x\) к) \(2\cdot b\cdot c\cdot \frac{\cos \frac{a}{2}}{b}+c\) л) \(4\cdot R\cdot \sin \frac{a}{2}\cdot \sin \frac{b}{2}\cdot \sin \frac{c}{2}\) м) \(a\cdot x+\frac{b}{c}\cdot x+d\) н) \(2\cdot \sin \frac{a+b}{2}\cdot \cos \frac{a-b}{2}\) о) \(|\sin (-3|\frac{x}{2}|)|\)
